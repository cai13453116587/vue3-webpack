import { Component } from 'vue';
import * as runtimeDom from '@vue/runtime-dom';
import { Router } from '../src';
export declare function mount(router: Router, Component: Component & {
    template: string;
    components?: Record<string, Component>;
}, rootProps?: {}): {
    app: runtimeDom.App<Element>;
    el: HTMLDivElement;
};
export declare const tick: () => Promise<unknown>;
