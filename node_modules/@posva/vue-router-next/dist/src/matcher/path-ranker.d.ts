import pathToRegexp from 'path-to-regexp';
import { RouteRecord } from '../types';
import { NormalizedRouteRecord, RouteRecordMatcher } from './types';
/**
 * Normalizes a RouteRecord into a MatchedRouteRecord. Creates a copy
 * @param record
 * @returns the normalized version
 */
export declare function normalizeRouteRecord(record: Readonly<RouteRecord>): NormalizedRouteRecord;
export declare function createRouteMatcher(record: Readonly<NormalizedRouteRecord>, parent: RouteRecordMatcher | void, options: pathToRegexp.RegExpOptions): RouteRecordMatcher;
